[package]
name = "io"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["client"] }
hyper-rustls = "0.23"
noun = { path = "../noun" }
rustls = "0.20"
tokio = { version = "1", features = ["io-std", "io-util", "macros", "rt"] }
